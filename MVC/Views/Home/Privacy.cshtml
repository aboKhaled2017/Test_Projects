@using MVC_Lib1;
@model List<MVC.Data.Person>
@{
    ViewData["Title"] = "Privacy Policy";
}
<h1>@ViewData["Title"]</h1>

@foreach (var p in Model)
{
    <div>
        Name: @p.Name <==> Age: @Calc.getDate(p.Age)
    </div>
}

<p>Use this page to detail your site's privacy policy.</p>
